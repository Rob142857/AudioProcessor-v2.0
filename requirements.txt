# ── Core transcription ──────────────────────────────────────────────
openai-whisper>=20250625
faster-whisper>=1.2.1

# ── Output ──────────────────────────────────────────────────────────
python-docx>=1.2.0                      # DOCX output generation

# ── System / progress ──────────────────────────────────────────────
psutil>=7.2.0
tqdm>=4.67.0

# ── PyTorch: install separately for your GPU ───────────────────────
# Your GTX 1070 Ti has CUDA 12.4 support via the installed torch 2.6.0+cu124.
# To reinstall / upgrade:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124
#
# CPU-only:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
